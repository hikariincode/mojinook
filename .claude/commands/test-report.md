---
description: Run tests, generate HTML report, and serve it locally
---

Run `npm run test:report:view`. Show me the full output, and once the local server starts, tell me to open `http://localhost:4173`. If it errors, show the complete error and suggest a fix.
